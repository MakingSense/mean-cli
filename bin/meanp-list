#!/usr/bin/env node

'use strict';

var cli = require('../lib/cli'),
    program = require('commander');

program
    .description('List all installed modules')
    .parse(process.argv);

cli.list();
